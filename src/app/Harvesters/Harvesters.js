import React, { Component } from 'react'
import PropTypes from 'prop-types'

export default class Harvesters extends Component {
  static propTypes = {}
  state = {}
  render () {
    return (
      <div>Harvesters</div>
    )
  }
}
